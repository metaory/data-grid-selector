<div id="mobile-warning" class="mobile-warning hidden">
  <iconify-icon icon="mdi:desktop-classic"></iconify-icon>
  <h3>Desktop Optimized</h3>
  <p>Best experienced with mouse and keyboard</p>
  <button onclick="this.parentElement.classList.add('hidden')">Got it</button>
</div>

<script is:inline>
  if ('ontouchstart' in window) {
    // document.getElementById('mobile-warning')?.classList.remove('hidden');
  }
</script>

<style>
  .mobile-warning {
    position: fixed;
    inset: 0;
    background: rgba(15, 23, 42, 0.8);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    backdrop-filter: blur(10px);
    padding: 2.5rem;
    text-align: center;
    gap: 1.5rem;
    max-width: 100vw;
    overflow: hidden;
  }

  .mobile-warning.hidden { display: none; }

  .mobile-warning iconify-icon {
    color: var(--accent-primary);
    filter: drop-shadow(0 0 20px rgba(96, 165, 250, 0.6));
    width: 48px;
    height: 48px;
  }

  .mobile-warning h3 {
    color: var(--accent-secondary);
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0;
  }

  .mobile-warning p {
    color: var(--text-primary);
    font-size: 1.1rem;
    line-height: 1.4;
    margin: 0;
    max-width: 300px;
  }

  .mobile-warning button {
    background: var(--accent-primary);
    color: var(--surface-primary);
    border: none;
    padding: 1rem 2rem;
    border-radius: 16px;
    font-weight: 500;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    backdrop-filter: blur(8px);
    box-shadow: 0 0 40px rgba(96, 165, 250, 0.4);
  }

  .mobile-warning button:hover {
    background: var(--accent-secondary);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(167, 139, 250, 0.4);
  }

  @media (max-width: 768px) {
    .mobile-warning {
      padding: 1.5rem;
    }
    
    .mobile-warning h3 {
      font-size: 1.3rem;
    }
    
    .mobile-warning p {
      font-size: 1rem;
      max-width: 280px;
    }
  }
</style>
